server:
  port: 8761

spring:
  application:
    name: eureka-server

# Standalone mode — do not self-register or fetch from peers
eureka:
  client:
    register-with-eureka: false
    fetch-registry: false
  server:
    enable-self-preservation: false
